<nav class="navbar navbar-fixed-top navbar-theme" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <%= link_to APP_SETTINGS['application_name'], root_path, class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if current_user %>
          <li>
            <%= link_to raw("#{fa_icon('twitter')} Tweets"), tweets_path %>
          </li>
          <li>
            <%= link_to raw("#{fa_icon('star')} Favorites"), favorites_path %>
          </li>
          <li>
            <%= link_to raw("#{fa_icon('search')} Search Terms"), search_terms_path %>
          </li>
        <% end %>
        <li>
          <%= link_to raw("#{fa_icon('bar-chart-o')} Plans"), plans_path %>
        </li>
        <!--
          <li class="active"><a href="#">Link</a></li>
        -->
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <p class="navbar-text"><%= current_user %></p>
          <li>
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
          </li>
        <% else %>
          <li>
            <%= link_to "Sign In", new_user_session_path %>
          </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->

  </div><!-- /.container-fluid -->
</nav>
